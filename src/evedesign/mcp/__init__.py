"""
Read-only MCP (Model Context Protocol) discovery server for evedesign.

Requires the mcp extra (pip install evedesign[mcp]) for the fastmcp dependency. See
mcp/README.md for setup instructions and mcp/server.py for the tool surface.
"""
